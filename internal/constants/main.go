// Package constants uses for constants
package constants

const (
	// ServerAddress uses for server ip
	ServerAddress = "0.0.0.0"
	// ServerPort uses for server port
	ServerPort = "8080"
	// LenShort uses for len of shorted link
	LenShort = 10
	// MaxTTL max TTL for link 10 days * 24 hours * 60 mins
	MaxTTL = 10 * 24 * 60
)
